<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bravedroid.playground">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CeruleanTheme.DayNight"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".HomeActivity"
            android:theme="@style/MyDashbordTheme"
            android:label="HomeActivity"
            android:exported="true" />
        <activity android:name=".DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".styles.ElementsActivity"
            android:exported="true" />
        <activity
            android:name=".notifications.SnackbarActivity"
            android:exported="true" />
        <activity
            android:name=".notifications.AlertDialogActivity"
            android:exported="true" />
        <activity
            android:name=".notifications.ToastActivity"
            android:exported="true" />
    </application>

</manifest>
